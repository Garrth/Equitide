---
phase: 03-co-rotation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/phase3/corot01_angular_momentum.py
  - analysis/phase3/outputs/corot01_angular_momentum_model.json
  - analysis/phase3/outputs/corot03_lift_preservation.json

interactive: false

conventions:
  units: "SI (m, kg, s, N, J, W, Pa, rad/s)"
  fluid: "fresh water rho_w=998.2 kg/m³, nu_w=1.004e-6 m²/s"
  co_rotation_fraction: "f in [0,1]: f = v_water_tangential / v_vessel_tangential"
  wall_friction_model: "Prandtl 1/5-power law: C_f = 0.074 * Re_wall^(-0.2), turbulent smooth wall"
  Re_wall_definition: "Re_wall = omega * R_tank^2 / nu_w (rotational Re for cylindrical wall)"
  maintenance_power: "P_corot = T_wall * omega = tau_w * A_wall * R_tank * omega"
  stall_limit: "lambda_max = 1.27 (from Phase 2); f_stall = 1 - lambda_design/lambda_max"
  power_formula: "P_drag = (1/2) * rho_w * C_D * A_frontal * v_rel^3 (cubic in v_rel — NOT v_rel^2)"
  lift_preservation: "v_rel_vertical = v_vessel_vertical = v_loop (vertical unaffected by horizontal co-rotation)"

dimensional_check:
  tau_w: "[Pa] = [kg/(m*s^2)]"
  T_wall: "[N*m]"
  P_corot: "[W]"
  f_ss: "[dimensionless]"
  Re_wall: "[dimensionless]"
  C_f: "[dimensionless]"

approximations:
  - name: "Taylor-Couette smooth-cylinder model"
    parameter: "vessel d_vessel = 0.457 m << tank circumference = 23 m"
    validity: "fill fraction ~60% (3 arms × 10 vessels × 0.457 m / 23 m); valid as upper bound on f_ss"
    breaks_when: "discrete-vessel correction needed for factor-of-2 accuracy; not needed for feasibility"
    check: "f_ss result labeled as upper bound; discrete-vessel correction noted as factor-of-2 uncertainty"
  - name: "Turbulent Prandtl 1/5-power C_f"
    parameter: "Re_wall >> 5e5"
    validity: "Re_wall = 0.913 * 3.66^2 / 1.004e-6 ~ 1.22e7 >> 5e5 (verified)"
    breaks_when: "laminar regime Re_wall < 5e5 (not applicable here)"
    check: "Verify C_f ~ 0.00181 at design Re_wall; turbulent regime confirmed"
  - name: "Quasi-steady co-rotation"
    parameter: "turbulent spin-up time tau_turb << operating time"
    validity: "tau_turb ~ R/u* ~ 4 s << hours of operation"
    breaks_when: "transient startup phase (minutes); steady-state energy balance unaffected"
    check: "Spin-up time estimate included in corot01 output"

contract:
  scope:
    question: >
      What steady-state co-rotation fraction f_ss does the 24-vessel geometry achieve, what is the
      power cost P_corot? Reconcile the factor-of-10 discrepancy between SUMMARY.md (~1.3 kW) and
      the Taylor-Couette estimate (~14 kW). Show that vertical relative velocity and hydrofoil lift
      are geometrically preserved under horizontal co-rotation.

  claims:
    - id: claim-fss-model
      statement: >
        The steady-state co-rotation fraction f_ss is computed from a self-consistent angular
        momentum balance (vessel drag torque input vs. wall friction torque) and is bounded above
        by the stall limit f_stall = 1 - lambda_design/lambda_max ≈ 0.29.
      deliverables: [deliv-corot01-json]
      acceptance_tests: [test-Pcorot-zero-at-f0, test-fss-below-stall, test-Pcorot-reconciled, test-dim-check-corot01]
      references: [ref-schlichting, ref-phase2-json, ref-phase1-json]

    - id: claim-lift-preserved
      statement: >
        Horizontal water co-rotation does not reduce vertical relative velocity; v_rel_vertical =
        v_loop remains unchanged for all f, preserving hydrofoil lift for all f < f_stall.
      deliverables: [deliv-corot03-json]
      acceptance_tests: [test-lift-preservation-geometric, test-lambda-eff-definition]
      references: [ref-phase2-json]

  deliverables:
    - id: deliv-corot01-json
      kind: data
      path: "analysis/phase3/outputs/corot01_angular_momentum_model.json"
      description: >
        Angular momentum balance model: f_ss (self-consistent upper bound), P_corot (derived from
        first principles), P_corot_range [lower, upper] with C_f uncertainty ±50%, spin-up time
        estimate, stall limit check, P_corot reconciliation with SUMMARY.md ~1.3 kW.
      must_contain:
        - "f_ss_upper_bound"
        - "P_corot_W"
        - "P_corot_range_W"
        - "Re_wall"
        - "C_f_nominal"
        - "tau_w_Pa"
        - "T_wall_Nm"
        - "f_stall"
        - "lambda_eff_at_fss"
        - "spin_up_time_s"
        - "SUMMARY_reconciliation"
        - "dimensional_checks"
        - "pitfall_guards"

    - id: deliv-corot03-json
      kind: data
      path: "analysis/phase3/outputs/corot03_lift_preservation.json"
      description: >
        Lift preservation geometric argument: velocity decomposition showing v_rel_vertical =
        v_loop unchanged for all f; lambda_eff definition; f_stall from lambda_eff = lambda_max.
      must_contain:
        - "velocity_decomposition"
        - "v_rel_vertical_preserved"
        - "v_rel_horizontal_reduced_factor"
        - "lambda_eff_formula"
        - "f_stall_calculation"
        - "lift_preservation_verdict"

  references:
    - id: ref-schlichting
      kind: other
      locator: "Schlichting & Gersten, Boundary Layer Theory 9th ed., §21.2"
      role: "method"
      why_it_matters: "C_f = 0.074 * Re^(-0.2) for turbulent flat plate; wall friction formula directly applicable to cylindrical tank wall."
      applies_to: [claim-fss-model]
      must_surface: true
      required_actions: ["cite", "use"]

    - id: ref-phase2-json
      kind: prior_artifact
      locator: "analysis/phase2/outputs/phase2_summary_table.json"
      role: "other"
      why_it_matters: "Provides omega_design, v_tan_design, v_loop, R_tank, lambda_design, lambda_max — load from JSON, do NOT hardcode."
      applies_to: [claim-fss-model, claim-lift-preserved]
      must_surface: true
      required_actions: ["read", "use"]

    - id: ref-phase1-json
      kind: prior_artifact
      locator: "analysis/phase1/outputs/phase1_summary_table.json"
      role: "other"
      why_it_matters: "Provides W_pump, W_buoy for system energy context."
      applies_to: [claim-fss-model]
      must_surface: true
      required_actions: ["read", "use"]

    - id: ref-greenspan-1963
      kind: paper
      locator: "Greenspan & Howard (1963), J. Fluid Mech. 17, 385-404"
      role: "must_consider"
      why_it_matters: "Classical spin-up theory; Ekman-layer timescale E^(-1/2) * Omega^(-1); basis for turbulent spin-up estimate."
      applies_to: [claim-fss-model]
      must_surface: true
      required_actions: ["cite"]

  acceptance_tests:
    - id: test-Pcorot-zero-at-f0
      subject: claim-fss-model
      kind: limiting_case
      procedure: >
        Set f=0 in the angular momentum balance. omega_water = f * omega_design = 0.
        Therefore tau_w = 0, T_wall = 0, P_corot = 0. Verify this is exact (not a small residual).
      pass_condition: "P_corot(f=0) = 0 to machine precision"
      evidence_required: [deliv-corot01-json]

    - id: test-fss-below-stall
      subject: claim-fss-model
      kind: consistency
      procedure: >
        Verify f_ss_upper_bound < f_stall = 1 - lambda_design/lambda_max.
        If f_ss > f_stall, result must be explicitly labeled as stall-limited and
        f_eff = f_stall used for downstream calculations.
      pass_condition: "f_ss_upper_bound < f_stall OR result labeled stall-limited with f_eff = f_stall"
      evidence_required: [deliv-corot01-json]

    - id: test-Pcorot-reconciled
      subject: claim-fss-model
      kind: benchmark
      procedure: >
        P_corot derived from: tau_w = 0.5*rho_w*C_f*(omega*R)^2;
        T_wall = tau_w * 2*pi*R*L * R; P_corot = T_wall * omega.
        Compare with SUMMARY.md ~1300 W. Document factor of discrepancy.
        P_corot must be in plausible range [1 kW, 20 kW] for smooth-wall Taylor-Couette at this Re.
      pass_condition: "P_corot derived independently from first principles; reconciliation note present; result in range [1 kW, 20 kW]"
      evidence_required: [deliv-corot01-json]

    - id: test-dim-check-corot01
      subject: claim-fss-model
      kind: dimensional_analysis
      procedure: >
        Verify all six: [tau_w] = Pa, [T_wall] = N*m, [P_corot] = W,
        [C_f] = dimensionless, [Re_wall] = dimensionless, [f_ss] = dimensionless.
        Check by explicit unit tracking in the derivation.
      pass_condition: "All six dimensional checks PASS in dimensional_checks block of JSON"
      evidence_required: [deliv-corot01-json]

    - id: test-lift-preservation-geometric
      subject: claim-lift-preserved
      kind: other
      procedure: >
        Explicit velocity decomposition: v_vessel = (v_tan [horizontal], v_loop [vertical]).
        v_water = (f*v_tan [horizontal], 0 [vertical]).
        v_rel = (v_tan*(1-f) [horizontal], v_loop [vertical]).
        Show v_rel_vertical = v_loop for ALL f — independent of co-rotation.
      pass_condition: "v_rel_vertical_preserved = true in JSON with explicit vector decomposition"
      evidence_required: [deliv-corot03-json]

    - id: test-lambda-eff-definition
      subject: claim-lift-preserved
      kind: consistency
      procedure: >
        Verify lambda_eff = v_tan / v_rel_horizontal = lambda_design / (1-f).
        Compute f_stall = 1 - lambda_design/lambda_max = 1 - 0.9/1.27.
        Expected f_stall = 0.291 ± 0.001.
        Both must agree between corot01 and corot03 JSON outputs.
      pass_condition: "f_stall = 0.291 ± 0.001; lambda_eff formula correct; values consistent between both JSONs"
      evidence_required: [deliv-corot01-json, deliv-corot03-json]

  forbidden_proxies:
    - id: fp-Pcorot-from-summary
      subject: claim-fss-model
      proxy: "Using the SUMMARY.md ~1300 W P_corot estimate as the Phase 3 value without re-derivation"
      reason: "Must derive from first principles using Taylor-Couette formula; SUMMARY.md has factor-of-10 discrepancy requiring reconciliation — silently carrying forward 1.3 kW is the Phase 3 pitfall PITFALL-C3 analogue"

    - id: fp-force-saving-as-power
      subject: claim-fss-model
      proxy: "Using (1-f)^2 as the power saving factor anywhere in the angular momentum or energy balance"
      reason: "Power scales as v_rel^3; force scales as v_rel^2. Power saving = [1-(1-f)^3]; using (1-f)^2 overcounts power saving by up to 30% at f=0.3 — PITFALL-C2 from Phase 2 applied to co-rotation"

    - id: fp-Pcorot-omitted
      subject: claim-fss-model
      proxy: "Reporting drag reduction without simultaneously reporting P_corot maintenance cost"
      reason: "PITFALL-C3 from project contract: co-rotation benefit and cost must always appear together; partial result inflates apparent COP gain"

    - id: fp-hardcoded-phase2-values
      subject: claim-fss-model
      proxy: "Hardcoding omega_design, v_tan, v_loop, R_tank, lambda_max from Phase 2 in the script"
      reason: "Pitfall C7 discipline from Phase 2; all Phase 1/2 values must be loaded from JSON files"

  uncertainty_markers:
    weakest_anchors:
      - "f_ss from smooth-cylinder approximation: factor-of-2 uncertainty from discrete-vessel geometry (fill fraction ~60%)"
      - "C_f from Prandtl 1/5-power law: ±30% vs. rougher wall models; ±50% range used for P_corot bounds"
      - "P_corot absolute value: factor-of-10 discrepancy with SUMMARY.md must be reconciled; result is order-of-magnitude only"
    disconfirming_observations:
      - "If f_ss > 0.5 from angular momentum balance, model has overestimated coupling — switch to parametric sweep (f_ss labeled unknown)"
      - "If P_corot > P_drag_full_total (~100 kW), wall friction model is unphysical — re-check A_wall formula"
      - "If f_ss < 0.05, co-rotation provides negligible drag reduction — Phase 3 net benefit is essentially zero"

estimated_execution:
  total_minutes: 45
  breakdown:
    - task: 1
      minutes: 35
      note: "Angular momentum balance derivation, P_corot computation, self-consistent f_ss, reconciliation"
    - task: 2
      minutes: 10
      note: "Lift preservation geometric argument — short kinematic derivation, JSON only"
---

<objective>
Derive the steady-state co-rotation fraction f_ss from a self-consistent angular momentum balance
(vessel drag torque input = wall friction torque), compute P_corot from first principles, reconcile
the factor-of-10 discrepancy with the SUMMARY.md estimate (~1.3 kW vs. ~14 kW Taylor-Couette),
and confirm that vertical relative velocity (and thus hydrofoil lift) is preserved under horizontal
co-rotation (COROT-03, kinematic argument).

Purpose: Establish the achievable co-rotation fraction and its energy cost before the net-benefit
sweep of Plan 02. COROT-03 is included here as a short geometric argument with no computational
dependency on Plan 02.

Output: corot01_angular_momentum_model.json (f_ss upper bound, P_corot with range, reconciliation)
and corot03_lift_preservation.json (velocity decomposition, v_rel_vertical preserved, f_stall).
</objective>

<execution_context>
@C:/Users/garth/.claude/get-physics-done/workflows/execute-plan.md
@C:/Users/garth/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/03-co-rotation/03-RESEARCH.md
@.gpd/phases/02-hydrofoil-torque/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-consistent angular momentum balance — derive f_ss and P_corot</name>
  <files>analysis/phase3/corot01_angular_momentum.py
analysis/phase3/outputs/corot01_angular_momentum_model.json</files>
  <action>
    Create analysis/phase3/corot01_angular_momentum.py using Phase 2 script conventions:
    ASSERT_CONVENTION header, all inputs from JSON, json output dict.

    ASSERT_CONVENTION header:
      unit_system=SI,
      co_rotation_fraction=f in [0,1] (f=v_water_h/v_vessel_h),
      power_formula=cubic P_drag=(1/2)*rho_w*C_D*A*v_rel^3,
      wall_friction=Prandtl_1/5_power C_f=0.074*Re_wall^(-0.2),
      Re_wall=omega*R_tank^2/nu_w,
      maintenance_power=P_corot=T_wall*omega,
      all_inputs_from_JSON (no hardcoding of Phase 1/2 values)

    Step 1 — Load inputs from JSON (Pitfall C7 guard):
      From analysis/phase2/outputs/phase2_summary_table.json:
        omega_design, v_tan_design, v_loop, R_tank, lambda_design, lambda_max
      From analysis/phase1/outputs/phase1_summary_table.json:
        W_pump_J, W_buoy_J
      Constants (from project geometry — not Phase 1/2 derived physics):
        L_tank = 18.288 m (60 ft, tank height)
        A_frontal = 0.164 m² (pi/4 * 0.457^2, from Phase 1 terminal velocity)
        C_D = 1.0 (bluff cylinder nominal)
        N_total = 24
        rho_w = 998.2, nu_w = 1.004e-6, A_wall = 2 * pi * R_tank * L_tank

    Step 2 — Wall friction at design omega (for reconciliation):
      Re_wall = omega_design * R_tank**2 / nu_w
      C_f_nominal = 0.074 * Re_wall**(-0.2)        [Schlichting §21.2]
      tau_w = 0.5 * rho_w * C_f_nominal * (omega_design * R_tank)**2
      T_wall = tau_w * A_wall * R_tank
      P_corot_nominal = T_wall * omega_design
      P_corot_lower = T_wall * 0.5 * omega_design  [C_f * 0.5 lower bound]
      P_corot_upper = T_wall * 2.0 * omega_design  [C_f * 2.0 upper bound]
      [Note: P_corot_lower/upper correspond to C_f scaled ±50%; T_wall scales accordingly]

    Step 3 — Reconcile with SUMMARY.md:
      SUMMARY_reference_W = 1300  [~1.3 kW from project SUMMARY.md]
      discrepancy_factor = P_corot_nominal / SUMMARY_reference_W
      SUMMARY_reconciliation note: document likely sources of discrepancy (C_f assumption,
      velocity reference, wall area assumption)

    Step 4 — Self-consistent f_ss via fixed-point / brentq:
      Define residual: g(f) = T_input(f) - T_wall_f(f)
        T_input(f) = N_total * 0.5*rho_w*C_D*A_frontal*(v_tan_design*(1-f))**2 * R_tank
        [vessel drag torque imparted to water at v_rel = v_tan*(1-f)]
        omega_water = f * omega_design
        If omega_water < 1e-10: T_wall_f = 0
        Else:
          Re_w = omega_water * R_tank**2 / nu_w
          C_f_w = 0.074 * Re_w**(-0.2) if Re_w > 5e5 else 0.0
          tau_f = 0.5 * rho_w * C_f_w * (omega_water * R_tank)**2
          T_wall_f = tau_f * A_wall * R_tank

      Check g(0) > 0 (T_input drives co-rotation) and g(0.999) <= 0 (wall friction dominates at high f).
      If g(0.999) > 0: f_ss = 1.0 (wall never dominates — model failure; report and flag).
      Else: use scipy.optimize.brentq on g(f) over [0, 0.999] to find f_ss.
      Label f_ss as f_ss_upper_bound (smooth-cylinder approximation overestimates coupling).

    Step 5 — Stall limit:
      lambda_max loaded from phase2_summary_table.json
      f_stall = 1 - lambda_design / lambda_max
      stall_flag = "STALL_LIMITED" if f_ss_upper_bound >= f_stall else "SAFE"
      f_eff = min(f_ss_upper_bound, f_stall)
      lambda_eff_at_fss = lambda_design / (1 - f_eff)

    Step 6 — Spin-up time estimate (turbulent):
      tau_w_val = 0.5 * rho_w * C_f_nominal * (omega_design * R_tank)**2
      u_star = (tau_w_val / rho_w)**0.5  [friction velocity]
      nu_T = 0.41 * u_star * R_tank      [turbulent eddy viscosity; kappa=0.41]
      tau_spinup = R_tank**2 / nu_T
      quasi_steady_valid = tau_spinup < 60  [if spin-up < 1 min, quasi-steady assumed valid]

    Step 7 — Limiting cases and dimensional checks:
      Verify T_wall at f=0: omega_water=0 → T_wall=0 (exact)
      Verify T_input at f=1: v_rel=0 → T_input=0
      dim_check: verify units at each step; record as dict of PASS/FAIL

    Step 8 — Write JSON:
      {
        "Re_wall": Re_wall, "C_f_nominal": C_f_nominal, "tau_w_Pa": tau_w,
        "T_wall_Nm": T_wall, "A_wall_m2": A_wall,
        "P_corot_W": P_corot_nominal, "P_corot_lower_W": P_corot_lower,
        "P_corot_upper_W": P_corot_upper, "P_corot_range_W": [P_corot_lower, P_corot_upper],
        "P_corot_SUMMARY_reference_W": 1300, "discrepancy_factor": discrepancy_factor,
        "SUMMARY_reconciliation": "...",
        "f_ss_upper_bound": f_ss, "f_stall": f_stall, "stall_flag": stall_flag,
        "f_eff": f_eff, "lambda_eff_at_fss": lambda_eff_at_fss,
        "spin_up_time_s": tau_spinup, "quasi_steady_valid": quasi_steady_valid,
        "limiting_cases": {"f0_T_wall": 0.0, "f0_T_wall_pass": true,
                           "f1_T_input": 0.0, "f1_T_input_pass": true},
        "dimensional_checks": {
          "tau_w_Pa": "PASS", "T_wall_Nm": "PASS", "P_corot_W": "PASS",
          "C_f_dimensionless": "PASS", "Re_wall_dimensionless": "PASS", "f_ss_dimensionless": "PASS"
        },
        "pitfall_guards": {
          "P_corot_derived_from_first_principles": true,
          "SUMMARY_1p3kW_NOT_used_as_Phase3_value": true,
          "power_formula_cubic_v_rel3": true,
          "phase2_inputs_loaded_from_JSON": true
        },
        "requirements_satisfied": ["COROT-01"]
      }
  </action>
  <verify>
    1. P_corot(f=0): T_wall(omega_water=0) = 0 exactly — confirmed in limiting_cases block
    2. Re_wall at design: expected ~1.22e7. Alert if outside [1e7, 1.5e7].
    3. C_f at Re_wall = 1.22e7: expected ~0.00181. Alert if outside [0.001, 0.003].
    4. P_corot order of magnitude: expected in range [1 kW, 20 kW]. If outside, re-check A_wall formula.
    5. discrepancy_factor vs. SUMMARY.md 1.3 kW: documented in SUMMARY_reconciliation field
    6. f_ss_upper_bound < f_stall = 0.291 OR stall_flag = "STALL_LIMITED"
    7. All 6 dimensional checks PASS in dimensional_checks block
    8. pitfall_guards: all four = true
    9. Power formula cubic: P_drag_full = 0.5*rho_w*C_D*A*v_tan^3 (three factors of v, not two)
    10. requirements_satisfied includes "COROT-01"
  </verify>
  <done>corot01_angular_momentum_model.json written with f_ss_upper_bound, P_corot (range),
Re_wall ~ 1.22e7, C_f ~ 0.00181, tau_w, T_wall, f_stall, spin-up time, SUMMARY reconciliation.
All 6 dimensional checks PASS. Limiting cases verified. pitfall_guards all true.</done>
</task>

<task type="auto">
  <name>Task 2: Lift preservation geometric argument (COROT-03)</name>
  <files>analysis/phase3/outputs/corot03_lift_preservation.json</files>
  <action>
    Write corot03_lift_preservation.json directly (no Python script — this is a kinematic argument).
    Load f_stall from corot01_angular_momentum_model.json to ensure consistency.

    Part A — Velocity decomposition:
      Vessel velocity: v_vessel_h = v_tan = lambda * v_loop [horizontal, tangential]
                       v_vessel_v = v_loop                  [vertical, ascent/descent]
      Water co-rotation: v_water_h = f * v_tan [horizontal only]
                         v_water_v = 0          [no vertical entrainment]
      Relative velocity: v_rel_h = v_tan * (1-f)   [reduced by factor (1-f)]
                         v_rel_v = v_loop            [UNCHANGED for all f]

    Part B — Hydrofoil lift argument:
      Total relative velocity: v_rel = sqrt((v_tan*(1-f))^2 + v_loop^2)
      Inflow angle: beta_eff = arctan(v_loop / (v_tan*(1-f))) [increases with f]
      Lift: L = 0.5 * rho_w * C_L * A_foil * v_rel^2
      Lift is nonzero as long as v_rel > 0. Since v_rel_v = v_loop > 0, lift is preserved
      for all f (even if v_rel_h → 0, v_rel = v_loop > 0 and AoA → 90° — which is stall).
      The stall constraint (not the lift formula itself) is what limits f.

    Part C — lambda_eff and f_stall:
      lambda_eff = v_tan / v_rel_h = lambda_design / (1-f)
      Load f_stall from corot01_angular_momentum_model.json (do NOT recompute independently)
      At f = f_stall: lambda_eff = lambda_max = 1.27 → AoA approaches stall angle ~15°

    Write JSON:
    {
      "velocity_decomposition": {
        "v_vessel_horizontal_ms": "v_tan = lambda * v_loop (from phase2_summary_table.json)",
        "v_vessel_vertical_ms": "v_loop (from phase2_summary_table.json)",
        "v_water_horizontal_ms": "f * v_tan (horizontal co-rotation only)",
        "v_water_vertical_ms": 0,
        "v_rel_horizontal_ms": "v_tan * (1-f)  [reduced by co-rotation]",
        "v_rel_vertical_ms": "v_loop  [UNCHANGED for all f]"
      },
      "v_rel_vertical_preserved": true,
      "v_rel_horizontal_reduced_factor": "(1-f)",
      "v_rel_vertical_reduced_factor": 1.0,
      "lift_preservation_argument": "v_rel_v = v_loop > 0 for all f; lift = 0.5*rho*C_L*A*v_rel^2 > 0 since v_rel >= v_loop > 0",
      "lambda_eff_formula": "lambda_design / (1-f)",
      "f_stall_calculation": {
        "lambda_design_from_phase2": "loaded from phase2_summary_table.json",
        "lambda_max_from_phase2": "loaded from phase2_summary_table.json",
        "formula": "f_stall = 1 - lambda_design / lambda_max",
        "f_stall_from_corot01": "loaded from corot01_angular_momentum_model.json"
      },
      "lift_preservation_verdict": "PRESERVED: v_rel_v = v_loop independent of f; lift nonzero for all f < f_stall",
      "stall_implication": "Stall at f_stall limits useful co-rotation range; above f_stall lambda_eff > lambda_max",
      "requirement": "COROT-03",
      "requirements_satisfied": ["COROT-03"]
    }
  </action>
  <verify>
    1. v_rel_vertical_preserved = true in JSON
    2. f_stall value matches corot01_angular_momentum_model.json (loaded, not independently hardcoded)
    3. lambda_eff_formula = "lambda_design / (1-f)" (consistent with Phase 2 convention)
    4. velocity_decomposition shows explicit split: horizontal (reduced by 1-f) and vertical (unchanged)
    5. lift_preservation_verdict explicitly states "v_rel_v = v_loop independent of f"
    6. requirements_satisfied includes "COROT-03"
    7. f_stall value ≈ 0.29 (within expected range given Phase 2 lambda_max = 1.27 and lambda_design = 0.9)
  </verify>
  <done>corot03_lift_preservation.json written with velocity decomposition, v_rel_vertical_preserved=true,
lambda_eff formula, f_stall loaded from corot01 JSON, lift_preservation_verdict PRESERVED.
COROT-03 requirement satisfied.</done>
</task>

</tasks>

<verification>
Plan-level physics consistency checks:

1. CUBIC power formula: Task 1 uses P_drag = 0.5*rho_w*C_D*A*v_rel^3 (three factors of velocity). Power formula is cubic in v_rel throughout — no instance of (1-f)^2 as a power factor anywhere.
2. f_stall consistency: f_stall computed in Task 1 from loaded lambda_max and lambda_design; same value loaded (not recomputed) in Task 2. Values must agree.
3. JSON loading: omega_design, v_tan, v_loop, R_tank, lambda_design, lambda_max all loaded from phase2_summary_table.json. W_pump, W_buoy from phase1_summary_table.json. No Phase 1/2 values hardcoded.
4. P_corot reconciliation: SUMMARY.md ~1.3 kW documented as reference; derived value used. discrepancy_factor and SUMMARY_reconciliation fields present.
5. Dimensional checks: All 6 quantities verified in dimensional_checks block.
6. pitfall_guards block confirms all four guards true.
7. COROT-01 and COROT-03 requirements both satisfied.
</verification>

<success_criteria>
- corot01_angular_momentum_model.json: f_ss_upper_bound, P_corot (W), P_corot_range_W, Re_wall ~ 1.22e7, C_f ~ 0.00181, tau_w (Pa), T_wall (N*m), f_stall ≈ 0.291, spin-up time, SUMMARY reconciliation note
- All 6 dimensional checks PASS
- Limiting case f=0: T_wall = 0 verified
- P_corot in plausible range [1 kW, 20 kW] for smooth-wall Taylor-Couette
- SUMMARY ~1.3 kW discrepancy documented with factor and explanation
- corot03_lift_preservation.json: v_rel_vertical_preserved=true, f_stall loaded from corot01, lambda_eff formula, lift_preservation_verdict PRESERVED
- COROT-01 and COROT-03 requirements satisfied
- pitfall_guards block: all four guards = true
</success_criteria>

<output>
After completion, create `.gpd/phases/03-co-rotation/03-01-SUMMARY.md` using the template at
`C:/Users/garth/.claude/get-physics-done/templates/summary.md`.

Key fields to populate:
- one-liner: f_ss and P_corot derived from angular momentum balance; SUMMARY reconciliation done; lift preservation confirmed (COROT-01, COROT-03)
- provides: f_ss_upper_bound (numerical value), P_corot_W (range [lower, upper]), f_stall = 0.291, corot01 and corot03 JSONs
- affects: Phase 3 Plan 02 (P_net sweep uses f_ss and P_corot from this plan)
- contract_results: claim-fss-model and claim-lift-preserved with evidence
</output>
